name: mvvm_provider_list
description: A Mason brick for a Flutter MVVM screen using Provider to fetch a list.
version: 0.1.0+1
vars:
  name:
    type: string
    description: Feature name (e.g., user, product)
    prompt: What is the name of the feature?