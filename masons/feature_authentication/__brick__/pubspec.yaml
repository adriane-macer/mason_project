name: {{package_name}}
description: Authentication feature package.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  dartz: ^0.10.1
  firebase_auth: ^5.5.0
  flutter_bloc: ^8.1.6
  equatable: ^2.0.8
  go_router: ^17.3.0
  get_it: any

  core_package:
    path: ../../core
  design_system:
    path: ../../design_system
  bootstrap_shared:
    path: ../../../packages/bootstraps/bootstrap_shared
  app_router:
    path: ../../app_router
  feature_user:
    path: ../../features/user/feature_user

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  bloc_test: ^9.1.7
