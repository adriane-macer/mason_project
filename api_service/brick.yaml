name: api_service
description: A new brick created with the Mason CLI.
version: 0.1.0+1

environment:
  mason: ^0.1.1

vars:
  name:
    type: string
    description: Api Service
    default: Api Service
    prompt: Enter initial name of the service?
